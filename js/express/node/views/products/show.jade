h1 Product Details:
- if (product)
  h2 #{product.name}
  p #{product.description}
  p #{product.price}
  p
    a(href='/products') Back

  p 
    a(href='/products/'+product.id + '/edit') Edit
- else
  p No product from input